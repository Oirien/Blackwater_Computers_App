{% extends 'base.jinja'%}

{% block content %}

<h1>Users</h1>

<main>
    <h2><a href="/users/new"> Create new user</a></h2>
  		<div>
  			{% for user in users %}
  				<ul>
    				<li><a href="/users/{{ user.id }}"> {{user.first_name}} {{user.last_name}}</a></li>
  				</ul>
  			{% endfor %}
  		</div>
</main>


{% endblock %}