{% extends 'base.jinja' %}

{% block content %}
    <h1>{{ type }}'s</h1>

    {% for component in components %}
        <p><a href="/components/show/{{ component.id }}">{{ component.name }}</a></p>
    {% endfor %}

{% endblock %}