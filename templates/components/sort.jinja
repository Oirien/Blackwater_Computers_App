{% extends 'base.jinja'%}

{% block content %}

<main>
    <div>
        {% for component in components %}
                <h1><a href="/components/sort/{{component.type}}">{{component.type}}</a></h1>
                <h2>{{component.description}}</h2>
        {% endfor %}
    </div>
</main>

{% endblock %}